{
  "name": "adolar-brand-images",
  "version": "0.5.0",
  "private": true,
  "description": "Brand assets, exports and animation system for the Adolar MZ-1 rocket.",
  "scripts": {
    "build": "node scripts/build-assets.mjs",
    "build:clean": "node scripts/build-assets.mjs --clean",
    "check": "node scripts/check-assets.mjs"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
