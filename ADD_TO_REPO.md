# Add these files to the repository

Copy the contents of this package into the repository root and run:

```bash
git add .
git commit -m "Add MZ-1 documentation and webplayer integration"
git push
```
