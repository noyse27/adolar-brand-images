# Add v0.2.0 files to the repository

Copy the contents of this package into the repository root and run:

```bash
git add .
git commit -m "Add v0.2.0 export set and project variants"
git tag v0.2.0
git push
git push origin v0.2.0
```

Release text is in `RELEASE-v0.2.0.md`.
